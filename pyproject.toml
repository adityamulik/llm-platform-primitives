[project]
name = "llm-platform-primitives"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.12"
dependencies = [
    "google-auth",
    "google-genai",
    "google-adk[extensions]",
    "litellm",
    "pyyaml",
]
